import React, { Component } from "react";
import { TableOrder } from "parts";

export default class Orders extends Component {
  render() {
    return (
      <div>
        <TableOrder />
      </div>
    );
  }
}
